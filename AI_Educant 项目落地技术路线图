这是一份为你量身定制的 **AI_Educant 项目落地技术路线图（执行版）**。

按照你“最核心、最重要的放前面”的要求，我们将完全抛弃传统的“前端先做、后端再做”的思路。在这个项目里，**没有数字底座，Agent 就是智障；没有 Agent，这只是个普通的搜题软件。**

因此，我们的开发顺序是：**数据底座 ➔ 智能体大脑 ➔ 业务中台 ➔ 前端与硬件交互**。

---

### 第一阶段：构建数字教育底座（绝对优先级：最高）
**目标**：让系统“理解”义务教育阶段的数学体系，建立对抗传统教辅的壁垒[cite: 1]。

#### 1. 知识图谱构建（Neo4j）
*   **用什么做**：`Neo4j`（图数据库） + `Python`（清洗与入库） + `DeepSeek-V3 API`（关系推导）。
*   **怎么做**：
    1.  **准备数据**：将义务教育数学（1-9年级）大纲整理为文本/CSV[cite: 1]。
    2.  **AI 打标脚本**：用 Python 写一个脚本，调用 DeepSeek API，将知识点解析为节点（Node），并推导出它们的前置/包含关系（Edges）。
    3.  **Cypher 写入**：
        ```cypher
        // 在 Neo4j 中执行，建立基础链接
        MERGE (a:KnowledgePoint {id: "math_7_01", name: "一元一次方程"})
        MERGE (b:KnowledgePoint {id: "math_8_05", name: "二元一次方程组"})
        MERGE (a)-[:PREREQUISITE_FOR]->(b)
        ```

#### 2. 权威题库向量化（Milvus）
*   **用什么做**：`Milvus` / `PGVector`（向量数据库） + `text-embedding-3-small` / `BGE-m3`（Embedding 模型）。
*   **怎么做**：
    1.  获取权威题库数据，确保每道题都有准确的**知识点 ID** 和**解析**[cite: 1]。
    2.  编写 Python 脚本，将题目文本转化为向量（Vector）。
    3.  存入 Milvus。**核心准则**：必须将对应的 `KnowledgePoint_ID` 作为标量（Scalar）字段一起存入，以便后续精准过滤，杜绝超纲和幻觉[cite: 1]。

---

### 第二阶段：开发智能体大脑（优先级：极高）
**目标**：开发一个能看懂错题、记住学情、自动编排试卷的 Python 微服务[cite: 1]。

#### 1. 学情记忆引擎（Mem0）
*   **用什么做**：`Mem0`（开源 Agent 记忆库） + `PostgreSQL`（底层存储）。
*   **怎么做**：
    1.  当学生做错题或拍照上传错题时，提取题目对应的知识点[cite: 1]。
    2.  调用 LLM 生成语义摘要（例如：“张三在处理分式方程的去分母环节容易忘记乘常数项”）。
    3.  调用 `mem0.add(text="...", user_id="user_123")` 将其写入用户的长期档案。

#### 2. 组卷决策流（LangGraph）
*   **用什么做**：`LangGraph` + `FastAPI`（暴露接口给 Java）。
*   **怎么做**：
    1.  **定义状态 (State)**：包含 `student_id`, `target_chapter`, `weak_points` 等变量。
    2.  **编写节点 (Nodes)**：
        *   `Retrieve_Profile_Node`：从 Mem0 读取用户薄弱点。
        *   `Graph_Search_Node`：去 Neo4j 查这些薄弱点的前置知识点（降级出题逻辑）。
        *   `Vector_Search_Node`：拿着这些知识点 ID，去 Milvus 里抽出 10 道题。
    3.  **启动服务**：用 `FastAPI` 包装这个 Graph，提供一个 `/api/v1/generate_paper` 接口。

---

### 第三阶段：搭建业务中台（优先级：高）
**目标**：处理 C 端用户的并发访问、收费（VIP体系）、鉴权以及调用 Python 大脑[cite: 1]。

#### 1. 核心业务 API（Java / SpringBoot）
*   **用什么做**：`SpringBoot 3` + `MySQL` + `Redis` + `MyBatis-Plus`。
*   **怎么做**：
    1.  **用户与鉴权**：实现 JWT 登录，建立 `user` 和 `vip_subscription` 表，控制免费与 VIP 用户的出题频次权限[cite: 1]。
    2.  **大桥逻辑**：Java 接收到前端的“一键出卷”请求后，带上 `user_id`，通过 HTTP Client 或 RPC 调用 Python 的 FastAPI 接口。
    3.  **结果缓存**：将 Python 返回的题目 ID 列表去 MySQL 查出完整题目图片/文本，存入 Redis 缓存，返回给前端。

#### 2. 排版与打印服务（Python / Java 混合）
*   **用什么做**：`ReportLab` (Python 渲染 PDF) 或基于模板的 HTML2PDF + 打印机厂商 SDK。
*   **怎么做**：
    1.  **生成试卷**：Python 拿到题目文本/图片后，自动渲染出带页眉、填空留白、标准边距的 A4 PDF 文件[cite: 1]。
    2.  **硬件下发**：SpringBoot 通过 MQTT 或厂商 SDK，将 PDF 文件的下载链接直接推送到用户绑定的专属打印机，实现“免调整格式，一键打印”[cite: 1]。

---

### 第四阶段：交互层与端侧体验（优先级：中）
**目标**：提供让家长和学生感到极度流畅的 UI 体验[cite: 1]。

#### 前端交互（Vue3 / 小程序）
*   **用什么做**：`Vue3` + `Vite` + `Pinia` / 微信小程序原生框架。
*   **怎么做**：
    1.  **拍照识图 (OCR) 模块**：集成阿里云 OCR 或类似服务，用户拍照后，前端将图片传给 Java 端，Java 再传给 Python 端提取知识点[cite: 1]。
    2.  **学情看板**：用 ECharts 渲染 Neo4j 中该学生的“雷达图”，直观展示知识点掌握情况，刺激家长续费 VIP[cite: 1]。
    3.  **试卷预览页**：支持在手机上预览 PDF 试卷，提供一个巨大的“连接打印机并打印”按钮。

---

### 💡 给开发者的落地建议（避坑指南）

1.  **不要在前期死磕 OCR 精度**：数学公式（LaTeX）的 OCR 是世界级难题。前期 MVP（最小可行性产品）版本，可以允许用户拍照后“手动点选”知识点，把精力全放在**Neo4j 的逻辑和组卷算法**上。
2.  **Python 与 Java 明确分工**：绝对不要在 Java 里写任何涉及 LangChain/向量库的代码，生态太差。Java 只负责 CRUD 和收钱，AI 运算全部交给 Python 微服务独立部署。
3.  **第一行代码从哪里开始写？**
    *   **今天晚上**：打开你的编辑器，写一段 Python 脚本，去调 DeepSeek API，把你们省份 7 年级上册数学目录转化成带 `PREREQUISITE_FOR` 关系的 JSON。这是这个百万元级项目的绝对基石。
```
